#include "blockchain.h"
#include <stdio.h>

// Creates a new transaction
void new_transaction() {

}

// Chooses a block to mine, and then mines it
void mine() {

}

int main() {
	printf("Hello World\n");
	while(1) {
		printf("Enter a value\n");
		char c[10];
		fgets(c, 10, stdin);
		printf("You have entered: ");
		puts(c);
	}
}
